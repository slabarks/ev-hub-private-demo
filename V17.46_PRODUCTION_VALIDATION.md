# V17.46 production validation

Validation date: 2026-07-11

Build under test: `EVHUB-V17.46-20260711-R1`

## 1. Exact production upload data

Source replayed: `Funded_Overview_Data_10_07_26.zip`.

### Browser-local parser result

| Check | Result |
|---|---:|
| Charger-level rows | 37,728 |
| Parsed site records | 45 |
| Sites with monthly history | 45 |
| Monthly observations | 430 |
| Latest actual date | 2026-07-09 |

The nine spreadsheets in the pack's `Overview` folder were also selected as individual files. The local parser returned the same 45 sites, 37,728 rows, 45 history sites, 430 observations and latest date.

### Active portfolio linkage

| Check | Result |
|---|---:|
| Active clean sites matched | 37 / 37 |
| Active sites with monthly history | 37 / 37 |
| Active-site monthly observations retained | 340 |
| Site records outside active portfolio | 8 |

The eight outside-active records were displayed as 2 excluded/retired and 6 additional or unmatched records.

## 2. Simulated legacy-backend deployment test

A browser session was run against a mock deployment where:

- `/api/import-live-calibration-v1746` returned 404; and
- `/api/import-live-calibration` returned a deliberately incomplete legacy response with zero monthly histories.

The exact production ZIP was selected through the Portfolio Financials file input.

Result:

- the incomplete backend response was not activated;
- the browser parsed `Daily_Charger_kWh.xlsx` locally;
- `Uploaded actuals active` appeared after 4 seconds in the test environment;
- Latest actuals date showed `2026-07-09`;
- Matched clean sites showed `37 / 37`;
- Monthly histories active showed `37 / 37` and `340 retained monthly observations`;
- Uploaded site rows showed `45`;
- no upload-error panel remained active.

This test reproduces the deployment failure shown in the user's V17.45 screenshots and confirms the upload no longer depends on the legacy backend returning monthly history.

## 3. Performance reconciliation

With production actuals active:

| Classification | Summary card | Site-row badges |
|---|---:|---:|
| In benchmark | 13 | 13 |
| Underperforming | 10 | 10 |
| Above benchmark | 14 | 14 |
| Review | 0 | 0 |

The summary and site table therefore use the same forward-12-month variance classification.

## 4. Rendering validation

The Portfolio Financials table was rendered in headless Chromium after the production ZIP upload.

### 1,920 px desktop viewport

- table width: 1,660 px
- table scroll width: 1,660 px
- wrapper width: 1,662 px
- wrapper scroll width: 1,660 px
- computed horizontal overflow: `visible`
- top synchronized scrollbar present: no
- all 10 first-row cells reported zero internal horizontal overflow

### 1,680 px desktop viewport

- table width: 1,565 px
- table scroll width: 1,565 px
- wrapper width: 1,567 px
- wrapper scroll width: 1,565 px
- right edge of final Run-rate payback cell: 1,615 px
- viewport right edge: 1,680 px

All ten columns were visible together with no horizontal scrollbar. Below 1,180 px the CSS changes the table rows into labelled responsive cards rather than creating horizontal scroll.

## 5. Automated regression suite

The final working tree passed `npm test`:

- syntax and static production guards;
- browser-local production upload parser regression;
- exact ZIP-pack parse;
- individual Overview-file parse in the validation environment;
- 18/18 AADT regression tests;
- 3/3 monthly live-data parser tests;
- forward-12-month and maturity separation;
- maturity and 20-year forecast regressions;
- 1–20-year projection controls;
- CAPEX directional formatting guards;
- performance/history separation guards;
- Portfolio Financials fit-to-width rendering guards;
- local `/api/version`, `/api/health`, AADT and V17.46 import endpoint smoke tests.

## Conclusion

V17.46 directly addresses the two failures reported against V17.45: monthly live history can be recovered from the selected production files in the browser even when the hosted Python backend is stale, and the 10-column financial table no longer requires horizontal scrolling on the validated desktop widths.
